/**
 * Même sous-ensemble que `STYLE_KINDLE` dans epubZipFromSpec (aperçu auteur, CDC Brique 4).
 */
export const EPUB_KINDLE_SAFE_SNIPPET = `@media amzn-kf8, amzn-mobi {
  body { margin: 0.4em 3%; font-size: 1em; }
}
body { font-family: Georgia, serif; }
`
