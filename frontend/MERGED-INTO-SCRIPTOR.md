# Ce dossier n’est plus utilisé

Le frontend IA a été **fusionné** dans **`scriptor/`** :

- Composants et API : `scriptor/src/ia/`
- Proxy `/api` → backend : `scriptor/vite.config.js`
- Tailwind (sans preflight global) : `scriptor/tailwind.config.js`

Tu peux supprimer ce dossier `frontend/` à la racine du workspace si tu n’en as plus besoin comme copie de secours.
